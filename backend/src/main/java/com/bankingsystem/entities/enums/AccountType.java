package com.bankingsystem.entities.enums;

/**
 * Tipo de cuenta bancaria (columna `account_type` en la tabla `account`,
 * V1__init_schema.sql).
 */
public enum AccountType {
    SAVINGS,
    CHECKING
}